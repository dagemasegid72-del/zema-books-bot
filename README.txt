Zema Books Bot — Telegram PDF storage

The PDF is NOT stored in GitHub.
Instead, upload the PDF to your Telegram bot once and put the resulting
Telegram file_id into the Render environment variable TELEGRAM_PDF_FILE_ID.

Required environment variables:
TELEGRAM_BOT_TOKEN
ADMIN_TELEGRAM_ID
TELEGRAM_PDF_FILE_ID
TELEBIRR_PAYMENT_DETAILS
